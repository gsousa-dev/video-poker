let cards = document.querySelectorAll('#cards > *');

let deck;

// window.onload = function() { loadDeck() };

function loadDeck() {
    // $.get('/video-poker/app/game/card-deck', (data) => {
    //     deck = JSON.parse(data);
    // });
}

function dealCards() {
    // deck.sort(function() { return 0.5 - Math.random() });
    
    // for (var i = 0; i < cards.length; i++) {
    //     cards[i].src = deck[i];   
    // }
}